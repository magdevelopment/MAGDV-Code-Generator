package ${packageName}.${subPackage}

import ru.terrakok.cicerone.Router
import javax.inject.Inject

class ${moduleName}RouterImpl @Inject constructor(
    private val appRouter: Router
) : ${moduleName}Router
